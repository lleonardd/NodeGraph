import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import NodeGraphPlayground from './NodeGraphPlayground'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NodeGraphPlayground />
  </React.StrictMode>
)
